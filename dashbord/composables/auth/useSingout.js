import { useUserStore } from '~/stores/user'
export default function () {

    const singout = () => {
        // const userStore = useUserStore()
        // userStore.setToken("")
    }

    return {singout}
}